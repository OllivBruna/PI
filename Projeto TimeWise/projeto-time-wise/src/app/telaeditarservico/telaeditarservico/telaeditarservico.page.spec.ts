import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TelaeditarservicoPage } from './telaeditarservico.page';

describe('TelaeditarservicoPage', () => {
  let component: TelaeditarservicoPage;
  let fixture: ComponentFixture<TelaeditarservicoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TelaeditarservicoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
