import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TelacriarservicoPage } from './telacriarservico.page';

describe('TelacriarservicoPage', () => {
  let component: TelacriarservicoPage;
  let fixture: ComponentFixture<TelacriarservicoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TelacriarservicoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
