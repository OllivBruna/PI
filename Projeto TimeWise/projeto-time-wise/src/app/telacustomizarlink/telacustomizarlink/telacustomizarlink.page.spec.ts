import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TelacustomizarlinkPage } from './telacustomizarlink.page';

describe('TelacustomizarlinkPage', () => {
  let component: TelacustomizarlinkPage;
  let fixture: ComponentFixture<TelacustomizarlinkPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TelacustomizarlinkPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
