import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TelacadastrarjuridicaPage } from './telacadastrarjuridica.page';

describe('TelacadastrarjuridicaPage', () => {
  let component: TelacadastrarjuridicaPage;
  let fixture: ComponentFixture<TelacadastrarjuridicaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TelacadastrarjuridicaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
