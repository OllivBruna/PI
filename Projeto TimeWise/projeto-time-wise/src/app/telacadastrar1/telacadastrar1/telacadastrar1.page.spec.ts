import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Telacadastrar1Page } from './telacadastrar1.page';

describe('Telacadastrar1Page', () => {
  let component: Telacadastrar1Page;
  let fixture: ComponentFixture<Telacadastrar1Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Telacadastrar1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
